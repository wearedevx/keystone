# Init project

ks init test-project

# Add secret to current env

#ks secrets add label value -c -o

#stdout 'Variable .*label.* is set for 1 environment\(s\)'

# ks secrets require label

#stdout 'toto'
